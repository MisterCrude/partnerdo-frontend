import React from 'react';

import Main from '@layouts/Main';

export const UserProfile: React.FC = () => {
    return (
        <div>
            <Main />
            UserProfile
        </div>
    );
};
