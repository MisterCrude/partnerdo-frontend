import React from 'react';

import Main from '@layouts/Main';

export const ProfileProposals: React.FC = () => {
    return (
        <div>
            <Main />
            ProfileProposals
        </div>
    );
};
